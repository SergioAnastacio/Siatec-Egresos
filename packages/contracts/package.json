{
  "name": "@siatec-egresos/contracts",
  "private": true,
  "version": "0.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "pnpm exec tsc -p tsconfig.json",
    "dev": "pnpm exec tsc -p tsconfig.json --watch --preserveWatchOutput",
    "lint": "node -e \"console.log('contracts: lint TODO')\"",
    "typecheck": "pnpm exec tsc -p tsconfig.json --noEmit",
    "test": "node -e \"console.log('contracts: no tests yet')\""
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
